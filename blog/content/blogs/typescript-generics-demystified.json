{
  "slug": "typescript-generics-demystified",
  "title": "TypeScript Generics Demystified",
  "tagline": "From basic type parameters to conditional types — a practical guide to writing type-safe, reusable TypeScript.",
  "category": "Concept",
  "date": "Apr 15, 2026",
  "readTime": "9 min read",
  "views": 2890,
  "thumbnail": "/thumbnails/tsgenerics.svg",
  "content": "## Why Generics?\n\nGenerics let you write code that works with **any type** while still being **type-safe**. They're the bridge between flexibility and safety.\n\n### The Basics\n\n```typescript\n// Without generics — not type-safe\nfunction firstElement(arr: any[]): any {\n  return arr[0];\n}\n\n// With generics — fully type-safe\nfunction firstElement<T>(arr: T[]): T | undefined {\n  return arr[0];\n}\n\nconst num = firstElement([1, 2, 3]);    // type: number\nconst str = firstElement(['a', 'b']);    // type: string\n```\n\n### Constraints\n\nUse `extends` to constrain what types are allowed:\n\n```typescript\nfunction getLength<T extends { length: number }>(item: T): number {\n  return item.length;\n}\n\ngetLength(\"hello\");     // ✅ string has .length\ngetLength([1, 2, 3]);   // ✅ array has .length\ngetLength(42);          // ❌ number doesn't have .length\n```\n\n### Real-World Pattern: API Response Wrapper\n\n```typescript\ntype ApiResponse<T> = {\n  data: T;\n  status: number;\n  timestamp: Date;\n  error?: string;\n};\n\nasync function fetchApi<T>(url: string): Promise<ApiResponse<T>> {\n  const response = await fetch(url);\n  const data = await response.json();\n  return {\n    data: data as T,\n    status: response.status,\n    timestamp: new Date(),\n  };\n}\n\n// Usage — fully typed!\nconst users = await fetchApi<User[]>('/api/users');\n// users.data is User[]\n```\n\n### Conditional Types\n\nThe power tool of TypeScript generics:\n\n```typescript\ntype IsString<T> = T extends string ? true : false;\n\ntype A = IsString<\"hello\">;  // true\ntype B = IsString<42>;       // false\n```\n\n### Utility Types You Should Know\n\n```typescript\n// Make all properties optional\ntype Partial<T> = { [P in keyof T]?: T[P] };\n\n// Make all properties required\ntype Required<T> = { [P in keyof T]-?: T[P] };\n\n// Pick specific properties\ntype Pick<T, K extends keyof T> = { [P in K]: T[P] };\n\n// Omit specific properties\ntype Omit<T, K extends keyof T> = Pick<T, Exclude<keyof T, K>>;\n```\n\n### The Golden Rule\n\nIf you find yourself writing `any`, ask: **\"Can I use a generic instead?\"** The answer is almost always yes."
}